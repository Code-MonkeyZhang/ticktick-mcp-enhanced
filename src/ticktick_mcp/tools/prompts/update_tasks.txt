Update one or more existing tasks.

Each task: task_id, project_id (required). Optional: title, content, start_date, due_date (ISO datetime with timezone; omit for all-day), time_zone, priority ("none"|"low"|"medium"|"high"), repeat_flag, items, reminders (iCal TRIGGER; pass [] to clear).
